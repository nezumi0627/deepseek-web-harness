# Skills

Put reusable skills in `skills/<name>/SKILL.md`.

The MCP tool accepts a `skills` array. Selected skill files are prepended to the
prompt before it is sent to DeepSeek Web.

Example:

```text
skills/code-review/SKILL.md
```
